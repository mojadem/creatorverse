function CreatorCard({ name, url, description, imageURL }) {
  return (
    <>
      <div>CreatorCard</div>
      <div>{name}</div>
      <div>{description}</div>
      <div>{url}</div>
      <div>{imageURL}</div>
    </>
  );
}

export default CreatorCard;
