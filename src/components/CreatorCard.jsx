function CreatorCard({ name, url, description, imageURL }) {
	return (
		<>
			<div>CreatorCard</div>
			<div>{name}</div>
			<div>{url}</div>
			<div>{description}</div>
			<div>{imageURL}</div>
		</>
	);
}

export default CreatorCard;
